<?php
// {"globalFunctions": "import", "globalConstants": {"PHP_*": "import", "DIRECTORY_SEPARATOR": "import"}}
namespace App;

use Foo\Bar;
use function array_map;
use function count;
use const PHP_VERSION;

class A
{
	function f($x)
	{
		count($x); strlen($x); in_array(1, $x); array_map('f', $x); dump($x); \Foo\count($x); Bar::count($x); $x->strlen();
		echo PHP_EOL . PHP_VERSION . DIRECTORY_SEPARATOR . M_PI . Bar::PHP_X;
	}
}
