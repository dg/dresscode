<?php
// {"chain": "nesting"}
$class->addMethod('__construct')
	->addPromotedParameter('container')
		->setPrivate()
		->setType($name);
$builder->addDefinition('templates')
	->setFactory(Engine::class)
		->addSetup('setCacheDirectory', [$dir])
		->addSetup('setLocale', [$locale]);
$a = $foo
	->bar()
	->baz();
$b = $foo
	->bar()
	->baz();
