2: Consecutive isset() calls must be combined into one
3: Consecutive isset() calls must be combined into one
3: Consecutive isset() calls must be combined into one
5: Consecutive isset() calls must be combined into one
7: Consecutive isset() calls must be combined into one
9: Consecutive isset() calls must be combined into one
