<?php
$a = \trim(...);
$b = \Foo\Bar::baz(...);
$c = $obj->run(...);
$d = $this->service->run(...);
$e = Foo::run(...);
$f = \Foo\Bar::run(...);
$g = $obj->run(...);
$h = Closure::fromCallable($callable);
$i = Closure::fromCallable([$obj, $name]);
$j = Closure::fromCallable('not a name');
$k = Closure::fromCallable([$obj, 'run', 'extra']);
$l = Closure::fromCallable(callable: [$obj, 'run']);
$m = Other::fromCallable([$obj, 'run']);
$n = Closure::bind($fn, $obj);
$lengths = array_map(fn($s) => strlen($s), $strings);
$trimmed = array_map(function ($s) { return trim($s); }, $strings);
$sorted = usort($items, fn($x, $y) => $this->compare($x, $y));
$static = array_map(Helper::make(...), $rows);
$o = fn($s) => strlen($s) + 1;
$p = fn($s, $t) => strlen($t);
$q = function ($s) { echo $s; return trim($s); };
$r = function ($s) use ($x) { return trim($s); };
$s = fn(&$x) => trim($x);
$t = fn($x = 1) => trim($x);
$u = fn($x) => trim($x, ' ');
$v = fn($x) => $x->trim();
$aw = Closure::fromCallable(["parent", "run"]);
$ax = fn($x) => parent::run($x);
$ay = fn($x) => self::run($x);
$az = function ($x) { return static::run($x); };
$ba = array_map(fn(string $s) => strlen($s), $strings);
$bb = array_map(fn($s): int => strlen($s), $strings);
